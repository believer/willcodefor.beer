<!DOCTYPE html>
<html lang="en">
  <head>
    {% seo "" %}
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="/style.css" />
    <link rel="shortcut icon" type="image/x-icon" href="/assets/favicon.ico" />

    {% block head %}{% endblock %}
    <script async data-no-cookie src="https://cdn.splitbee.io/sb.js"></script>
  </head>
  <body>
    <main class="mt-8 md:px-8 grid grid-template-main">
      <div
        class="flex flex-col md:flex-row md:items-center md:justify-end col-start-3 col-end-4 space-y-2 md:space-y-0 md:space-x-8 border-b md:border-0 border-gray-200 pb-8 md:pb-0"
      >
        <a
          class="font-bold text-indigo-600 dark:text-indigo-400 {{ 'nav-item' if '/' === page.url }}"
          href="/"
        >
          @rnattochdag
        </a>
        <a
          class="font-bold {{ 'nav-item' if '/uses' in page.url }}"
          href="/posts"
        >
          Posts
        </a>
        <a
          class="font-bold {{ 'nav-item' if '/uses' in page.url }}"
          href="https://devlog.willcodefor.beer"
          >Devlog</a
        >
        <a
          class="font-bold {{ 'nav-item' if '/uses' in page.url }}"
          href="/uses"
        >
          What I Use
        </a>
      </div>
      <div class="my-10 md:my-32 col-start-3 col-end-4">
        {% block content %}
        {{ content | safe }}
        {% endblock %}
      </div>
    </main>
  </body>
</html>
